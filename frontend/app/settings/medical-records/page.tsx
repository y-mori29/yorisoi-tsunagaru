import { StubScreen } from "@/components/screens/_stub/StubScreen";

export default function MedicalRecordsPage() {
  return (
    <StubScreen
      title="診察の記録（便利機能）"
      summary={"声で 簡単に、診察の メモを 残せる ようにします。\n\n（あとで 文字に 起こして、次の 通院に 持って いけるように）"}
    />
  );
}
