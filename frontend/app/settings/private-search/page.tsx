import { StubScreen } from "@/components/screens/_stub/StubScreen";

export default function PrivateSearchPage() {
  return (
    <StubScreen
      title="検索されないようにする"
      summary={"名前で 探されても、\n出てこない ように します。\n\n（お隣さがしの 結果には 出ますが、\n名前で 直接は 見つけられなくなります。）"}
    />
  );
}
